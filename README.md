### Production React w/ SSR & Vite

Template for React + TS feat. SSR via Vite (Rollup gang)

### Quick Start

1. Clone this repository
1. `yarn && yarn dev` to run the dev server
1. Alternatively, to run a production server: `yarn start:clean`
